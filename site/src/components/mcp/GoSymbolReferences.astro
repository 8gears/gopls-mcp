---
import '../../styles/mcp-demo.css';
---

<div class="comparison-container">
  <div class="comparison-grid">

    {/* === LEFT: MANUAL === */}
    <div class="workflow-pane manual-theme">
      <div class="pane-header">
        <span class="pane-title">Text Search</span>
        <div class="badge error">No MCP</div>
      </div>

      <div class="pane-content">
        <div class="step-item">
          <div class="step-marker">1</div>
          <div class="step-text">Grep "Save"</div>
        </div>

        <div class="terminal-mock">
          <div class="term-line"><span class="prompt">$</span>grep -r ".Save(" .</div>
          <div class="term-line">main.go: u.Save()</div>
          <div class="term-line">file.go: // TODO...</div>
          <div class="term-line">
            <span class="term-err">cfg.Save() &lt;- False Pos.</span>
          </div>
        </div>

        <div class="friction-note">
          ⚠️ Noisy (String match)
        </div>
      </div>
    </div>

    {/* === VS === */}
    <div class="vs-divider">
      <div class="vs-line"></div>
      <div class="vs-text">VS</div>
      <div class="vs-line"></div>
    </div>

    {/* === RIGHT: MCP === */}
    <div class="workflow-pane mcp-theme">
      <div class="pane-header">
        <span class="pane-title">Semantic Refs</span>
        <div class="badge success">With MCP</div>
      </div>

      {/* 去掉 centered-content，让内容自然从上到下排列 */}
      <div class="pane-content">
        <div class="code-context">
          <pre class="code-snippet">func (u *User) <span class="highlight-trigger">Save</span>() error</pre>
        </div>

        <div class="connection-visual">
          <div class="arrow-down"></div>
          <div class="mcp-pill">find references</div>
        </div>

        <div class="result-card">
          <div class="result-header">
            <span class="res-title">User.Save</span>
            <span class="res-loc">2 refs</span>
          </div>
          <div class="result-body">
            <div class="ref-item">
              <span class="ref-file">auth/login.go:42</span>
              <span class="ref-code">user.Save()</span>
            </div>
            <div class="ref-item">
              <span class="ref-file">cmd/worker.go:15</span>
              <span class="ref-code">u.Save()</span>
            </div>
          </div>
        </div>

        <div class="efficiency-note">
          🎯 100% Accurate
        </div>
      </div>
    </div>

  </div>
</div>