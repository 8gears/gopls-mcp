{
  "workdir": "/path/to/your/project",

  "max_response_bytes": 32000,

  "gopls": {
    "staticcheck": true
  }
}

# USAGE EXAMPLES:

# 1. Use global limit (32KB):
#    analyze_workspace(cwd="/path/to/project")
#
# NOTE: The response size limit is controlled globally via the max_response_bytes
# config option. Per-request overrides are not currently supported.
